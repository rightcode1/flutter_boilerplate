import 'package:flutter/material.dart';

/// 기본 색상 상수들을 정의한다. 현재 파일 내에 존재하는 색상 상수는 학원나우 컬러에서 가져왔다.
/// 필요에 따라 기존 색상 상수를 제거, 추가 또는 수정하여 사용한다.

// 진한 색상 (주로 title - font Katuri 로 많이 사용 )
const PRIMARY_COLOR_DARK = Color(0xFF024c77); // (2, 76, 119)
// 주색상
const PRIMARY_COLOR = Color(0xFF48a5e2); // (72, 165, 226)
// 연한 색상 (학원 등록, 배차, + 자녀 등록 버튼)
const PRIMARY_COLOR_LIGHT = Color(0xFF80b2e4); // (128, 178, 228)

// 힌트 텍스트 컬러
const HINT_TEXT_COLOR = Color(0xffa0a0a0);

const TEXT_FILED_COLOR = Color(0xFFfcfcfc);

// 노란색 자녀 로그인, 수정
const YELLOW_COLOR = Color(0xffF1AB19); // (241, 171, 25)

// 핑크색 하원 text
const PINK_COLOR = Color(0xffDB385C); // (219, 56, 92)

// 환불 버튼 background 색상
const REFUND_BUTTON_BG_COLOR = Color(0xffFFE0E7); // (255, 224, 231)

// 흰색 Container 배경 색상 홈 버튼
const CONTAINER_BG_COLOR = Color(0xfff8f8f8); // (248, 248, 248)
// 테두리, 구분선 색상 (튜토리얼 이전 버튼 색상)
const BORDER_COLOR = Color(0xffe9ebef); // (233, 235, 239)

const HOME_TOP_BUTTON_BORDER_COLOR = Color(0xffd3d3d3); // (211, 211, 211)

// 회원가입 버튼 background 색상
const SIGN_UP_BUTTON_BG_COLOR = Color(0xff414141); // (65, 65, 65)
// 확인하기, 중복검사, 인증 버튼 background 색상
const AUTH_BUTTON_BG_COLOR = Color(0xff797979); // (121, 121, 121)
// dialog 취소 버튼 background 색상
const DIALOG_CANCEL_BUTTON_BG_COLOR = Color(0xffa0a0a0); // (160, 160, 160)

// 홈 버튼 중간 text 색상
const HOME_BUTTON_CENTER_TEXT_COLOR = Color(0xff6b797e); // (107, 121, 126)

// 홈 버튼 하단 text 색상
const HOME_BUTTON_BOTTOM_TEXT_COLOR = Color(0xff414141); // (65, 65, 65)
